use crate::loader::JavaScriptSource;

#[derive(Debug)]
pub struct Message {
    pub file_path: String,
    // pub import_set: std::collections::HashSet<String>,
    // pub source: JavaScriptSource<'a>,
}
